[project]
name = "webgis-medical-plants-api"
version = "0.0.0"
requires-python = ">3.14"

[tool.ruff]
target-version = "py314"
extend-exclude = [
    "alembic",
]

[tool.ruff.lint]
extend-select = [
    "B",
    "I",
    "UP",
    "FAST",
    "ASYNC",
]
